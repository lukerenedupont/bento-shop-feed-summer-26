# macOS
.DS_Store

# Purl local metadata & run artifacts
.purl/
.purl-run/

# Build products
DerivedData/
.derivedData/
.build/
build/
.build-cache/
DemoArtifacts/
*.xcresult/
xcuserdata/
*.xcuserdata
*.xcuserdatad
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/

# Local Python caches
__pycache__/
*.py[cod]

# Local credentials — keep out of every handoff.
.env
.env.*
*.env
!.env.example
!.env.sample

# Cover source art (200MB+ of working files; the app bundles downscaled
# imagesets in Assets.xcassets, so the repo doesn't need these)
covers/

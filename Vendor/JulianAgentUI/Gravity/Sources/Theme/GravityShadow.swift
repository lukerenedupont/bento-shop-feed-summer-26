// Generated by packages/shop-native-swiftui/scripts/theme/extract-gravity-theme.py. Do not edit by hand.

import SwiftUI

public struct GravityShadowAttributes: Sendable {
    public let color: Color
    public let radius: CGFloat
    public let x: CGFloat
    public let y: CGFloat
}

public enum GravityShadowLevel: String, CaseIterable, Sendable {
    case none
    case s
    case androidXs
    case androidS
    case m
    case l

    public var attributes: GravityShadowAttributes {
        switch self {
        case .none: return GravityShadowAttributes(color: .clear, radius: 0, x: 0, y: 0)
        case .s: return GravityShadowAttributes(color: GravityColor.bgFillFixedDark.opacity(0.06), radius: 8, x: 0, y: 2)
        case .androidXs: return GravityShadowAttributes(color: GravityColor.bgOverlayFixedDark40.opacity(0.06), radius: 4, x: 0, y: 2)
        case .androidS: return GravityShadowAttributes(color: GravityColor.bgOverlayFixedDark40.opacity(0.04), radius: 6, x: 0, y: 4)
        case .m: return GravityShadowAttributes(color: GravityColor.bgFillFixedDark.opacity(0.12), radius: 24, x: 0, y: 4)
        case .l: return GravityShadowAttributes(color: GravityColor.bgFillFixedDark.opacity(0.24), radius: 40, x: 0, y: 8)
        }
    }
}

private struct GravityShadowModifier: ViewModifier {
    let level: GravityShadowLevel

    func body(content: Content) -> some View {
        let attributes = level.attributes
        return content.shadow(
            color: attributes.color,
            radius: attributes.radius,
            x: attributes.x,
            y: attributes.y
        )
    }
}

public extension View {
    func gravityShadow(_ level: GravityShadowLevel = .none) -> some View {
        modifier(GravityShadowModifier(level: level))
    }

    func gravityShadow(_ level: GravityShadowLevel?) -> some View {
        gravityShadow(level ?? .none)
    }
}
